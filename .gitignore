# === Environment ===
.env
.env.*
!.env.example

# === Python ===
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# === Distributable build artifacts (fresh-machine .dmg: seed dump, image
#     tarballs, tiles, slope raster). Multi-GB, reproducible from infra/packaging/*. ===
release/

# === Node / JS / TS ===
node_modules/
.next/
.expo/
.expo-shared/
dist-ssr/
*.local
.eslintcache

# === Vite ===
dist/

# === Mobile (RN + Expo) ===
ios/Pods/
ios/build/
android/build/
android/app/build/

# === Map / source data (download locally; NEVER commit) ===
# All ingested datasets (USGS, MRDS, BLM, USFS, CGS, etc.) are downloaded per
# machine and stay local — the repo ships the code that downloads, not the data.
tiles/*
!tiles/config.json
# Map label fonts (Noto Sans Regular glyphs) ARE tracked — a tiny, fixed
# dependency the contour elevation labels need, not a per-machine build artifact
# like the .mbtiles. Committing them keeps labels working offline / on a fresh
# checkout, in line with the offline-first design.
!tiles/fonts/
*.mbtiles
data/
backend/data/
*.shp
*.shx
*.dbf
*.prj
*.gpkg
*.geojson
!**/fixtures/**.geojson

# === Logs ===
*.log
logs/

# === macOS ===
.DS_Store
.AppleDouble
.LSOverride

# === IDE ===
.vscode/
.idea/
*.swp

# === LangSmith trace exports ===
.langsmith/

# === Eval cache ===
evals/_snapshots_cache/

# === Local design / demo docs (never committed) ===
mockups/
DEMO-SCRIPT.md

# graphify knowledge-graph output (generated, not source)
graphify-out/
